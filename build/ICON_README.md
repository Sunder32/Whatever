# Icon Placeholder

This is a placeholder file. 

To build the Windows executable, you need to convert `icon.svg` to `icon.ico`:

1. Visit https://convertio.co/svg-ico/
2. Upload `build/icon.svg`
3. Download as `icon.ico`
4. Place it in the `build/` folder

For now, the build will use the default Electron icon.
